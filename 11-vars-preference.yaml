- name: variables preference
  hosts: local
  connection: local
  vars:
    NAME: ansible from vars
  vars_files:
    - vars.yaml
  vars_prompt:
    - name: NAME
      prompt: "Enter your name"
      private: no
  # tasks:
  #   - name: print the variable
  #     vars:
  #       NAME: ansible from task
  #     ansible.builtin.debug:        
  #       msg: "value coming from: {{ NAME }}"

#First priority: Dynamicaly passing argument
#Second priority: Variable defined in task
#Third priority: Variable defined from File "vars.yaml"